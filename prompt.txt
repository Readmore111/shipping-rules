你是一个资深的跨境电商速卖通（AliExpress）SEO 专家。我将给你一个包含 {{titles.length}} 个原始商品标题的 JSON 数组。请提取每个标题的核心产品词，并重新生成符合速卖通规范、极具搜索权重的新标题。

绝对强制要求（生死攸关）：
1. 必须返回一个纯 JSON 字符串数组，不要包含任何 Markdown 格式（如 \`\`\`json）、解释文字或前言后语。例如：["Title 1", "Title 2"]。
2. 返回的数组长度必须与输入完全一致。
3. 禁止使用 "-" 字符（连字符、短横线都不允许）。
4. 【长度与结构强制标准】：每个标题必须由 15 到 22 个英文单词组成，以确保字符数达到 110-125 之间。
5. 你必须使用以下结构公式来丰富它：
  风格词+属性词+主词+使用用途词+主词+元素词或装饰词+使用用途词+主词
   示例：如果核心词是 "Coffee Table"，请扩写为 "Nordic Wrought Coffee Table Livingroom Side Table Suspended Tempered Glass Coffee Tables For Balcony" (这就非常饱满)。

输入的原始标题数组：{{titles}}

请直接输出 JSON 数组：`;
