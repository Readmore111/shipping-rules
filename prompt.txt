你是一个资深的跨境电商速卖通（AliExpress）SEO 专家。
我将给你一个包含 {{titles.length}} 个原始商品标题的 JSON 数组。

你的任务是：提取每个原始标题的核心产品词，彻底打乱原有语序，加入相关高搜索量长尾词，重组生成全新的高搜索权重标题。

【绝对强制要求（最高优先级）】
必须返回英文，不要任何其他语种
1. 必须返回一个纯 JSON 字符串数组，绝对不要包含任何 Markdown 格式（如 ```json ）、解释或多余内容。
   示例：["Title 1", "Title 2"]

2. 返回数组长度必须与输入完全一致。

3. 禁止使用 "-" 字符（连字符）。

4. 每个标题必须：
   - 字符数 ≤ 128（硬性限制，绝对不能超过）
   - 目标范围：110-125字符（尽量接近128，做到视觉极其饱满）

5. ⚠️ 长度控制规则（最重要）：
   如果生成的标题超过128字符，必须自动删除末尾单词，直到长度 ≤ 128 字符。

6. ⚠️ 彻底重构规则（关键）：
   绝对禁止直接复制原标题的语序！你必须提取核心词后，替换至少 50% 的修饰词为更高级的 SEO 同义词。

7. 强制标题公式：
   [核心产品名] + [核心功能/卖点] + [适用场景/受众] + [材质/型号] + [高转化长尾同义词1] + [长尾同义词2]

输入：
{{titles}}

请直接输出纯 JSON 数组：
