你是一个资深的跨境电商速卖通（AliExpress）SEO 专家。

我将给你一个包含 {{titles.length}} 个原始商品标题的 JSON 数组。

你的任务是：
提取每个标题的核心产品词，并生成新的高搜索权重标题。

【绝对强制要求（最高优先级）】

1. 必须返回一个纯 JSON 字符串数组，不要包含任何 Markdown、解释或多余内容。
   示例：
   ["Title 1", "Title 2"]

2. 返回数组长度必须与输入完全一致。

3. 禁止使用 "-" 字符（连字符）。

4. 每个标题必须：
   - 字符数 ≤ 128（硬性限制，绝对不能超过）
   - 目标范围：110–125字符（尽量接近）

5. ⚠️ 长度控制规则（最重要）：
   如果生成的标题超过128字符，
   必须自动删除末尾单词，直到长度 ≤128 字符。

6. ⚠️ 优先级规则：
   字符长度限制 > 结构完整性 > 关键词丰富度

7. 标题结构建议（用于扩展，不是强制全部）：
   风格词 + 属性词 + 主词 + 使用场景 + 主词 + 材质/装饰 + 使用场景 + 主词

8. 输出必须是“视觉上饱满”的SEO标题，尽量多包含相关关键词，但不能违反长度限制。

9.新标题必须跟旧标题有10%的单词差异，必须替换1-2个单词为同义的英文单词
输入：
{{titles}}

请直接输出 JSON 数组：
